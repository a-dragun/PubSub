<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= room.name %> - Detalji</title>
  <link rel="stylesheet" href="/admin/room.css">
</head>
<body>

  <div class="room-box">
    <h1><%= room.name %></h1>
    <ul>
      <li><strong>Tip sobe:</strong> <%= room.type %></li>
      <li><strong>Vrijeme za odgovor:</strong> <%= room.timeToAnswer %> sekundi</li>
      <li><strong>Vrijeme između pitanja:</strong> <%= room.timeBetweenQuestions %> sekundi</li>
      <li><strong>Bodova po točnom odgovoru:</strong> <%= room.points %></li>
      <li><strong>Stvoreno:</strong> <%= room.createdAt.toLocaleDateString('hr-HR') %></li>
      <li><strong>Kategorije:</strong>
        <% room.categories.forEach(cat => { %>
          <span class="category"><%= cat %></span>
        <% }); %>
      </li>
    </ul>

    <form id="deleteForm" action="/admin/rooms/<%= room._id %>?_method=DELETE" method="POST">
      <button type="submit" onclick="return confirmDelete()">Obriši sobu</button>
    </form>
  </div>

  <script>
    function confirmDelete() {
      return confirm("Jesi li siguran da želiš obrisati sobu? Akcija nije reverzibilna.");
    }
  </script>

</body>
</html>
